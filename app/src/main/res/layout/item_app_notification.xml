<?xml version="1.0" encoding="utf-8"?>
<!-- Compact, modern notification item design - lightweight and efficient -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/notificationContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:gravity="center_vertical">

    <!-- Left accent line for unread indicator -->
    <View
        android:id="@+id/unreadIndicator"
        android:layout_width="3dp"
        android:layout_height="32dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/rounded_accent_line"
        android:visibility="gone" />

    <!-- Small circular icon -->
    <FrameLayout
        android:id="@+id/iconBackground"
        android:layout_width="36dp"
        android:layout_height="36dp">

        <ImageView
            android:id="@+id/ivNotificationIcon"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_nav_notifications"
            app:tint="@color/primary" />

    </FrameLayout>

    <!-- Content - compact single line with message -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <!-- Title and time in single row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvNotificationTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Transaction Added"
                android:textColor="@color/text_primary"
                android:textSize="13sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvNotificationTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="2h"
                android:textColor="@color/text_hint"
                android:textSize="11sp" />

        </LinearLayout>

        <!-- Message - single line -->
        <TextView
            android:id="@+id/tvNotificationMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Food expense of $50"
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />

    </LinearLayout>

    <!-- Type indicator dot -->
    <TextView
        android:id="@+id/tvNotificationType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:paddingHorizontal="8dp"
        android:paddingVertical="3dp"
        android:text="New"
        android:textColor="@color/income_green"
        android:textSize="9sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <!-- Small delete button -->
    <ImageView
        android:id="@+id/btnDeleteNotification"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginStart="4dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="6dp"
        android:src="@drawable/ic_close"
        app:tint="@color/text_hint" />

</LinearLayout>
